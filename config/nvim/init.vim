set nocompatible " Disable compatibility to old-time vi
set ignorecase " Do case insensitive matching
set hlsearch " highlight search results (unhighight with :noh)

call plug#begin()
Plug 'tpope/vim-rails'
Plug 'vim-ruby/vim-ruby'
call plug#end()
