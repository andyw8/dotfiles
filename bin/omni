#!/bin/sh

# detect current folder.
script_path=$(dirname "$0")
# get the first argument as the task.
task="$1"

# invoke apple script and pass entry to it.
osascript "$script_path/omni.scpt" "$task"
