if v:version < 704
  echo "Warning - old Vim version"
endif

" https://github.com/nelstrom/homebrew-vim
if !has("patch-7.4.865")
  echo "Warning - cdo command may not be available"
end

if !has('clipboard')
  echo "Warning - clipboard not enabled"
endif

if !executable('ag')
  echo "Warning - ag is missing"
end
